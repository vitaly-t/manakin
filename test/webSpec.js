'use strict';

var lib = require('../manakin');
var capture = require('./capture');

describe("formatting", function () {
    /*
     * Nothing as of yet, we are only testing the Node.js part of the library.
     * */
});
